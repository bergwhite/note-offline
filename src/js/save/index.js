const init = require('./init')
const add = require('./add')
const del = require('./add')
const mod = require('./mod')
const template = require('./template')
module.exports = {
  init: init,
  add: add,
  del: del,
  mod: mod,
  template: template
}
